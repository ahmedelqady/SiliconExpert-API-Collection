# Keep only reusable Spec Kit Codex prompt assets in repo.
# Avoid committing any future local Codex state/secrets if CODEX_HOME points here.
.codex/*
!.codex/prompts/
!.codex/prompts/*.md

# macOS metadata
.DS_Store

# Generated sync artifacts (uploaded to GitHub Actions, not committed except by bot)
artifacts/
!artifacts/.gitkeep

# Node
node_modules/
